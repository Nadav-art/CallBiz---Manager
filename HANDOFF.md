# CallBiz Manager — Handoff (2026-07-26)

פרוטוטייפ CRM/תיאום חי, RTL עברית, vanilla HTML/CSS/JS, ללא פריימוורק. פוקוס נוכחי: **ליטוש ממשק מחשב (דסקטופ)** + התכנסות לקוד אחד למובייל+מחשב (מרקאפ יחיד, פריסה רספונסיבית ב-CSS).

## פרסום
- **קישור קנוני יחיד (לעדכן בכל build):** https://claude.ai/code/artifact/a2ed325c-0cf7-467b-a732-4c565ebcf64f
- קובץ build עצמאי: `dist/callbiz-app.html` (וגם `dist/callbiz-mobile.html` = קישור ישן 455b2cbf).
- **build:** מריצים את סקריפט ה-Python המוטמע שקורא את `index.html`, מטמיע inline את כל ה-CSS/JS (כולל `mobile.css`), וכותב ל-`dist/callbiz-app.html`. אחרי כל שינוי: מעלים `?v=NNN` בכל התגיות ב-`index.html` (`sed -i 's/?v=OLD/?v=NEW/g'`), reload ל-`http://localhost:4173/`, בודקים, build, ואז `Artifact publish` עם ה-URL הקנוני.
- dev server מקומי: `http://localhost:8777/index.html` (של המשתמש) · בדיקות ב-`http://localhost:4173/`.
- גרסה נוכחית: v239.
- **מודולים נפרדים חדשים (נתיקים):** `js/clock.js` (מנוע זמן-ייחוס + `setClockLive(true)` למעבר לזמן-אמת אסיה/ירושלים), `js/permissions.js`+`css/permissions.css` (טאב הרשאות), `js/branch-settings.js`+`css/branch-settings.css` (חלון סניף). `js/workhours-settings.js`+css (חלון שעות פעילות).

## בדיקות
- Browser pane headless: rAF/timers מווסתים, screenshots נכשלים ב-iframe → אימות דרך `javascript_tool` (מדידות + matches). מובייל = `document.documentElement.classList.add('cb-mobile')` (≤820px); דסקטופ = resize_window ל-1280×800 + `classList.remove('cb-mobile')`.

## מבנה קבצים עיקרי
- `js/app.js` — ליבה: רשימת לידים (`listRowHTML`/`bindListRow`), Drawer (`openDrawer`/`closeDrawer`/`flowHTML`/`accItem`/`computeStages`), פעולות מהירות+עין (`qaEdit`), התראות פעמון (`openAlertsDrawer`/`alertsList`/`updateBellBadge`/`NOTIF_LOG`), העברת טיפול (`openTransferLead`), כרטיס זימון (`ensureMeetingMeta`), `openSchedule`, `scrollDrawerToActiveStage`, פופ-אפ התראת שורה (`rowAlertHTML`/`showAlertPop`/`bindRowAlertPop`), `renderNav`/`renderTabs`/`openSetModeMenu`.
- `js/views.js` — הגדרות (`renderSettingsView` + מצבים: עובד/mgr/branches/catalog/fields), שדות היררכיים (`renderFieldsManagerView`/`WF_FIELD_MAP`), קטלוג (`renderCatalogView`/`openCatalogItemEditor` — כולל new mode), סניפים, יומן (`calShift`/`calWeekHTML` — חלון מתגלגל), משימות (`renderTasksView`/`openAddTask`), שעות פעילות (`workHoursCard`).
- `js/flows.js` — עורך "הפעולה הבאה" (`openNextActionEditor` — שני dropdowns: סוג + דחייה, דיפולט מ-`FOLLOWUP_POLICY`), hover card (`showHoverCard`/`attachHoverCard`), ניהול שדות (`LEAD_FIELDS`/`LEAD_FIELD_ORDER`).
- `js/coord.js` — לוח תיאום (`renderCoordView`/`openCoordForRecord`) + **`openCoordOverlay`/`closeCoordOverlay`/`coordViewHost` (קוד רדום, לא מחווט עדיין)**.
- `js/schedule.js` — מסך זמנים מוצעים (`openProposedTimes`/`renderProposedTimes` — טראק self/service, דיפולט: שירות נבחר→איש מקצוע, אחרת→מולי), `openSelfMeetingDetails`.
- `js/services.js` — קטלוג-כתצורה, בחירת שירות (`renderServiceSelect` — מסונן להרחבות אם `journey==='upsell'`), בירור צורך דינמי (`needsQuestions`).
- `js/dock.js` — צ'אט + `pushNotify` (chat→#pushHost שמאל-תחתון דסקטופ, reminder→#pushHostReminder תחתון-רחב), `NOTIF_LOG`.
- `js/meetings.js` — יומן פגישות מבוסס-תפקידים (`renderMeetingsView` — cal+agenda תמיד ב-DOM, CSS מציג לפי meetView בנייד).
- `js/data.js` — `WORKFLOWS` (upsell עודכן: request/contact/need/service/proposal/...), `NEXT_ACTIONS`, `FU_POLICY_OPTIONS`(8, multi), `FOLLOWUP_POLICY.modes`, `CAL_STEP`, ICONS (נוסף org/lock), `DEPT_MAP`/`deptOf`.

## הושלם ופורסם בסשן זה (חלקי — מהמשמעותיים)
העברת טיפול · כפתור חזרה בליד · פעמון עם מונה+היסטוריה · טאב ניהול נגיש בנייד · גיליון "הפעולה הבאה" (→ הומר ל-2 dropdowns קומפקטיים עם דיפולט מהגדרות העובד) · מתג פגישה מולי/איש-מקצוע · מסך הגדרות שדות היררכי (וורקפלואו›קטגוריה›שדות + "יומן") · קטלוג ככרטיסים + עורך מלא + "הרחבת שירות" · טופס הוספה לקטלוג עם כל השדות · יומן מתגלגל יום-יום בחצים · חיווי "טרם מולא" בשורה (פופ-אפ צף בריחוף במחשב, ליד הסטטוס) · תיקון וי בשלב שטרם התחיל · גוון ורוד אחיד לשורה שעבר זמנה · גלילה אוטומטית לשלב הפעיל · העין נסגרת בין לידים (וצבע מותג כשלחוצה) · התראת צ'אט שמאל-תחתון מעל אייקון הצ'אט · **תיקון קריטי: פאנל proposePanel סגור לא חוטף לחיצות (pointer-events:none) + modal z-index 320** · "שנה מועד" פותח פופ-אפ זמנים (לא יומן) · na-pop רוחב 300 (תאריך לא נחתך) · לוגיקה אחידה ל"הפעולה הבאה" לפי מחלקה · כיווץ שלבים שהושלמו + נעץ per-user (`cb_pinStages_<user>`) · מסלול הרחבת שירות = כמו מכירה (contact/need/service/proposal) + בחירת שירות מסוננת להרחבות · איחוד קוד: עובדים+קטלוג+יומן פגישות מרקאפ יחיד רספונסיבי.

## הושלם ב-v167→v173 (המשך הסשן)
- **חלון "הגדרות שעות פעילות" מלא ומודולרי** — `js/workhours-settings.js` + `css/workhours-settings.css` (קבצים נפרדים בכוונה). `WHS_PANELS` = רישום פאנלים, כל פאנל render()+bind() עצמאי: כללי / שעות פעילות / הפסקות / סוגי שירות / תורים מקסימליים / חגים וחופשות / הגדרות נוספות. סיידבר ימני, טופ-בר (24ש׳/תצוגה שבועית/ניווט/העתק שבוע), פוטר (אזור זמן+שמירה אוטומטית). `openWorkHoursSettings()`. `whsEnsureAllDays()` מנרמל ל-7 ימים. רענון דרך `whRefresh()` (חלון→`whsRenderPanel`, אחרת→`renderSettingsView`).
- **תיקון כל הכפתורים המתים בהגדרות** (מהאודיט) — קיבולת/חלונות-פולו-אפ עריכים, חריגים הוסף/מחק, מדיניות-אישור+סנכרון toggles אמיתיים, ניתוב/עדיפות עריכים (הוסף/הסר). הכל בתוך פאנלי החלון.
- **יומן מוטבע בהגדרות-העובד** — `workHoursCard()` מציג את היומן ישירות (7 עמודות אחידות) + כפתור "פתח מסך מלא". שאר כרטיסי-התזמון הישנים הוסרו מהמסך (עברו לחלון).
- **עיצוב כרטיס-יום סופי** — כותרת: קבב(⋮)+שם+מתג עליון (`showDayKebab`: כבה/נקה-הפסקות/העתק-לימים). בלוק מקטע נקי: שעון בכותרת הבלוק, טווח שעות ממורכז ללא אייקונים inline; מתג `.wh-toggle` תואם-עיצוב. Grid = `repeat(7,minmax(124px,1fr))` אחיד.
- **בחירת שירות — 3 כפתורים** (`renderServiceSelect`): ביטול · **בחירה** (שומר `r.services` בלי תיאום — להצעת מחיר/פגישה) · בחירה והמשך לתיאום.
- **הרחבת שירות ב-2 סוגים** (`openCatalogItemEditor`): `extType` = `addon` (תוספת טיפולים) / `complement` (מוצר משלים + `complementsOf[]` בחירת מוצרים). העורך עבר ל-render/bind פנימי (בלי איבוד עריכות).
- **זמן-ייחוס לפיתוח** — `COORD_TODAY` (let) + `APP_NOW.min`; `nowHM()` מבוסס עליהם. תג `#devClock` (פינה תחתונה, `initDevClock` ב-app.js) לעריכת תאריך+שעה. פותר את "כל המסך אדום".
- **פופ-אפ אישור ממורכז `cbConfirm`** (מ-v166) + הסרת confirm נייטיב מ"שנה מועד".

## ⏳ פתוח שנותר מהסשן (לא בוצע):
- **מסך סניפים — כניסה לסניף** (בקשת המשתמש): כל סניף לחיץ→כרטיס/חלון סניף; **שדה "שם סניף"** עצמאי (לא נגזר מהעיר; להוסיף `b.name`); בתוך הסניף **יומן עם פיצולים + הגדרות שירות פר-פיצול** (למשל מבוגרים בבוקר/ילדים בצהריים, חוגי בוקר לצעירים/ערב למבוגרים). לשקול שימוש חוזר בשלד `.whs-*` המודולרי. `renderBranchManagerView`/`branchCardHTML`/`bindBranchManager` ב-views.js; מודל ב-`COORD_BRANCHES` (data.js:618).

## הושלם ב-v166 (סשן נוכחי)
- **כרטיס hover (#1)** — הוסר מספר הטלפון מהכותרת (נשאר שם+ארגון) ושורת תאריך הפגישה; נוספה שורת **SLA** צבועה לפי דחיפות (`.hc-sla` ב-`v17.css`). קוד: `showHoverCard` ב-`flows.js`.
- **אישור "שנה מועד" כפופ-אפ ממורכז** (בקשת המשתמש) — הוסר ה-`confirm()` הנייטיב מ-`openSchedule` (`app.js`); "שנה מועד" נפתח **ישר** לפופ-אפ הזמנים כדי לעיין בזמנים חלופיים בלי להתחייב. רק בבחירת מועד בפועל (`data-ptbook` / self-`smSave` ב-`schedule.js`) מוקפץ **`cbConfirm`** — פופ-אפ אישור ממורכז עצמאי (`#cbConfirmWrap`, z-400, CSS ב-`v20.css`, helper ב-`app.js` ליד `closeModal`). כפתור "עדכן תור"; ביטול/רקע/Esc סוגר ומשאיר את פאנל הזמנים. `r.meeting` מתעדכן בפורמט `DD/MM/YY` דרך `ptFmtDate`.
- **שעות פעילות — עיצוב מחדש שלב א׳ (#8 חלקי)** — הוחלף `workHoursCard`/`whDayRow` הישן (רשימה אנכית) ב-`whDayCard` — **ימים ככרטיסים ב-grid שממלא את רוחב הדף** (`.wh-daysgrid`, `views.js`). לכל יום **ציר-זמן ויזואלי** (`whTimeline` — בלוק מותג=עבודה, ענבר=הפסקה). **מתג "הצג 24 שעות"** (`wh24`, `#wh24Toggle`) מרחיב את הציר ל-00:00–24:00 עם סימוני שעות. **שורת פעולות מהירות** (`.wh-quickbar`): סמן/בטל הכל, "החל יום ראשון על כולם". CSS ב-`v20.css`. בינדינג ב-`bindWorkHours` (מתרנדר-מחדש דרך `renderSettingsView` שקורא bindWorkHours בסוף). **נותר ב-#8:** סרגל צד 8-מקטעים · אותו עיצוב גם בכרטיס הסניף · הרשאות (עובד/מנהל-אזור/מנהל-על). ⚠ למשתמש יש מוקאפ בצילומים — לבקש ליישור מדויק.

## BACKLOG פתוח (לפי סדר שהמשתמש ביקש; דסקטופ אלא אם צוין)
2. **פעמון**: המונה יראה רק **לא-נראו** (לא לרדת 8→5 אחרי פתיחה — לסמן הכל כ"נראה" בפתיחה, badge=לא-נראו); ולפתוח כ**פופ-אפ במרכז המסך** (modal) ולא כמגירת צד ימנית.
3. **יישור שורת סטטוס** — להעביר את ⚠ ל**ימין** בתא הסטטוס עם slot ברוחב קבוע כך שכל צ'יפי הסטטוס יתחילו באותו קו (עם/בלי התראה).
4. **הצעת מחיר** — בכל "הצעת מחיר": סיכום שירותים/מוצרים שנבחרו + סיכום עלויות + **הנחות/הטבות לפי הגדרות בקטלוג** (להוסיף לקטלוג הגדרת הטבות/הנחות).
5. **הצעת מחיר בהרחבת שירות** — משקללת את השירותים שנבחרו (svcTotals); הסינון להרחבות נעשה, החישוב בהצעה — פתוח.
6. **"יומן מלא" כשכבה נשלפת משמאל במחשב** — הקוד רדום ב-coord.js (`openCoordOverlay`/`closeCoordOverlay`/`coordViewHost`, `renderCoordView` כבר תומך ב-host). חסר: לחווט `data-open="calendar"` ל-`openCoordOverlay` במחשב + CSS ל-`#coordOverlay` (slide-in משמאל, z<220 כדי שה-Drawer יישאר מימין) + כפתור "יומן מלא" בתוך פופ-אפ הזמנים.
7. **גרירת יומן בעכבר** — mousedown+drag על תא ריק מזיז ימים (החצים כבר מזיזים יום-יום).
8. **עיצוב "שעות פעילות" מחדש (גדול)** — ✅ שלב א׳ בוצע ב-v167 (כרטיסים בשורה + ציר-זמן + מתג 24 שעות + פעולות מהירות, בכרטיס העובד). **נותר:** סרגל צד (כללי/שעות פעילות/הפסקות/סוגי שירות/חדרים/תורים מקסימליים/חגים וחופשות/הגדרות נוספות); אותו עיצוב **גם בכרטיס הסניף**; הרשאות: עובד רואה סניפים משויכים, מנהל-אזור רואה אחראים, מנהל-על רואה הכל (מוקאפ קיים בצילומי המשתמש — לבקש ליישור מדויק).
9. **מומחיות/סניפים >8 אפשרויות** — להציג ~8 ראשונים + פופ-אפ לבחירת הכל (במקום שורה ארוכה); ערך שניתן לקלוט בסינון חיצוני.
10. **תצוגת רשימה לעובדים** (מתג כרטיסים/רשימה) + יישור כפתורי ההגדרות.
11. **"כללי ניתוב ועדיפות"** — להפוך צ'יפים ללחיצים/toggle + אפשרות הוספה.
12. **חריגים** — כפתור "הוסף" שיעבוד (תאריך + תווית + סוג).
13. **תבנית אישור הגעה (וואטסאפ)** — להעביר מהגדרות עובד ל**אוטומציות**.
14. **קריטריונים לפי תחום (#13)** — בירור צורך: שירות→קריטריוני תקלות, גבייה→קריטריוני גבייה; הגדרה פר-וורקפלואו בהגדרות.
15. **הרשאות סניפים** — עובד רואה רק פרטי סניפים משויכים, מנהל-אזור רואה אחראים, מנהל-על רואה הכל (גם בכרטיס עובד וגם בכרטיס סניף).

## הערות עבודה מהמשתמש (feedback חשוב)
- מובייל כבר עובד טוב — עדכונים מסוימים **רק לדסקטופ**.
- לא לשבור את מה שכבר עבד; רק התאמת ממשקים.
- להתכנס לקוד אחד (מרקאפ יחיד + CSS רספונסיבי) במקום ענפי JS לפי `cb-mobile`.
- פופ-אפ צף אחד בלבד בו-זמנית.

## v174–v213 — מה נוסף מאז
- **שעון ייחוס (`js/clock.js`)** — `CLOCK.live=false`, עוגן דמו 23/05/2024, `setClockLive(true)` להשקה (Asia/Jerusalem). באדג' פיתוח לתאריך+שעה.
- **קריטריונים כמקור אמת יחיד (`js/needs-criteria.js`)** — רישום מאסטר + `t.critKeys` בקטלוג (בלי כפילויות), חסימת סניף עם סיבה, הגדרת משך שיריון.
- **גאו (`js/geo.js`)** — קואורדינטות ערים אמיתיות, Haversine, "הסניף הקרוב" כאופציה בשאלת הסניף.
- **סניפים (`js/branch-settings.js`)** — כניסה לסניף: כללי (כתובת+אימות+מפה), שעות פעילות עם פיצולים, שירותים/נגישות/קריטריונים.
- **שיריון וסליקה (`js/schedule.js`)** — באנר משך למעלה + 3 מסלולים (שיריון / הסכם לחתימה / סליקה), שעון יורד בליד, 8 אוטומציות שיריון.
- **פרופיל עסקה (`js/stage-profiles.js`)** — 3 שכבות נראות שלבים (חסימה / כבוי כברירת מחדל / רלוונטיות אוטומטית מהקטלוג) + אחראי לכל שלב.
- **חוזים** — `contracts.js` (תבניות+משתנים+פלטת משתנים), `contract-wizard.js` (סעיפים משפטיים, שם ללקוח מול שם פנימי, סליקה מטוקנת, חתימה), `contracts-registry.js` (מרשם, סטטוסים, נעילת גרסה, מסלול ביקורת).
- **שני קהלי חוזים (`js/contracts-audience.js`, v213)** — מערכת אחת, לשוניות "הסכמי לקוחות" / "הסכמי העסקה" לפי ההרשאה **ניהול הסכמי עובדים**. משתני עובד (`CONTRACT_EMP_VARS`) נשאבים מתיק ה-HR, תבניות e1/e2, הפקה מתוך HR › מסמכי עובד (`openEmployeeContractPicker`), והמרשם מסונן לפי קהל.
- **HR (`js/hr.js`)** — 6 לשוניות: תיק עובד, העסקה ותפקיד, נוכחות וחופשות, הכשרות, מסמכים, קליטה/סיום.
- **הרשאות (`js/permissions.js`)** — מטריצה לפי תפקיד, חריגים לפי עובד, HR, ו**הגדרות מנהל** במקום אחד.
- **סדר בהגדרות (`js/views.js`)** — קיבוץ ל-אנשים / מקומות / תוכן.
- **היקף וכללי קריטריונים (`js/criteria-rules.js`, v214)** — לכל קריטריון: לשונית **היקף** (שירותים בקטלוג → `t.critKeys` · סניפים אוטומטי/נבחרים → `CRIT_SCOPE` · מטריצת סניף×ערך → `b.brCriteria`) ולשונית **כללים** לכל ערך בנפרד + גורף + **"אחר"** (ברירת מחדל: תשלום מלא). סוגי כללים ב-`CRIT_EFFECTS`: תשלום · תמחור · תורים משוריינים · אחריות · מסמכים · אישור מראש · משך · הערה · כללים נוספים. ההשלכות מוצגות ב-`critEffectsBarHTML` (ליד · הצעת זמנים · סליקה) והמחיר זורם ל-`holdSlotForPayment`.
- **קריטריונים נגזרים (`js/criteria-derived.js`, v216)** — `CRIT_KINDS`: list · age · since · count · decl. **טווח = ערך**, ולכן כל מנוע הכללים חל עליו. `critDerive` מחשב מ-`r.critData` מול שעון הייחוס; `critSyncDerived` כותב ל-`r.needs`. כללים חדשים: **ביצוע/חסימה** ו**חריגה באישור** (`critApplyOverride`), ודגל **"כלל גובר"** (force) בתשלום/תמחור. `critBookGuard` חוסם תיאום בפועל ב-`data-ptbook`. הצהרת בריאות = שאלון (`openCritDecl`) עם חתימת נציג וחותמת זמן; התוצאה נשמרת כערך (עבר/דורש אישור/נפסל). שיוך הקריטריונים לשירותים ולסניפים נשמר כמראה מתמידה ב-`CRIT_SCOPE.svcs/brVals` ומוחזר לזיכרון ב-`critRestoreScope()`.
- **פעמון במקום משולש (v217)** — `ICONS.bell` חדש; משמש בכפתור ההתראות בסרגל העליון ובכותרת מגירת ההתראות. משולש ה-alert נשאר בכל שאר המקומות (אזהרות נקודתיות).
- **שיוך משמרת לסניף (`js/shift-branch.js`, v217)** — עובד עם 2+ סניפים חייב לשייך כל משמרת. אחסון נפרד מ-`workSchedule`: `SHIFT_BRANCH = { empId: { 'יום:אינדקס': branchKey } }` ב-`cb_shiftBranch`. בורר בכל מקטע משמרת (הפסקות פטורות), סימון `.wh-seg.br-miss`, באנר `whBranchBannerHTML` עם שיוך-הכל וסיכום שעות שבועי לפי סניף. עובד עם סניף אחד → שיוך משתמע ותווית סטטית. הסרת סניף מהעובד מבטלת שיוכים לאותו סניף.
- **משמרת מחזורית (`js/shift-rotation.js`, v218)** — לכל משמרת: תדירות (כל שבוע / אחת ל-2,3,4) + חלון תוקף (מ־/עד). `SHIFT_ROT[empId]['יום:si'] = {every,offset,from,to}` ב-`cb_shiftRot`; "השבוע הפעיל" לפי `rotWeekIndex` מעוגן ל-`ROT_ANCHOR='2024-01-07'`. **אינטגרציה אמיתית:** `coordBuildPool` מדלג דרך `rotDocBlocked(docKey,date,branch)` — בשבוע ה"לא" פשוט אין משבצות לאותו מטפל **באותו סניף** (משמרות שלו בסניף אחר לא נפגעות). בורר בצ׳יפ בתוך המשמרת עם תצוגת תאריכים קרובים; סיכום ב-`whb-rot`.
- **חלון תיאום מהפגישה הקודמת (`js/criteria-gap.js`, v222)** — סוג קריטריון `gap`: מרווח מינימלי ו/או מועד אחרון (מספר + ימים/שבועות/חודשים/שנים) מהפגישה הקודמת. `gapWindow(def,r)` מחשב [prev+min, prev+max]; שלושת המצבים (`GAP_VALS`: מוקדם מדי / בתוך החלון / חלף המועד) הם **ערכי הקריטריון**, ולכן כל מנוע הכללים חל עליהם. `gapCheckDate(r,date)` מסמן כל משבצת בהצעת הזמנים — חסומה (block) או בתשלום (pay) — ו-`data-ptbook` מסרב לקבוע במועד חסום. `gapBarHTML` מציג את החלון. ברירות מחדל נזרעות ב-`gapSeedRules`. הרחבות סוג נטענות דרך hooks ב-`critDefinePanelHTML`/`bindCritDefinePanel`/`critDerive` (`panel`/`panelBind`/`derive`/`initFn`).
- **יחידות זמן + מקור אוטומטי (v221)** — `CRIT_UNITS`, `CRIT_BASIS`, `critLastVisit(r)` (מ-`r.lastVisit` או `r.meeting.date` בעבר). שם הקריטריון מתמלא אוטומטית מהסוג.
- **מובייל: שעות פעילות (v220)** — `.cb-mobile .wh-daysgrid { grid-template-columns: 1fr }` + כרטיס ליום.
- **מונה התראות (v219)** — `BELL_SEEN` ב-`cb_bellSeen`; `bellNew()` סופר רק חדשות, `bellMarkSeen()` מאפס בפתיחה.
- **שאלון התאמה (`js/criteria-fit.js`, v223)** — סוג קריטריון `fit`. לכל שאלה: מה קורה בתשובה שלילית — `down` (מוריד לחלופה) או `disq` (פוסל). שלוש תוצאות (`FIT_VALS`) = ערכי הקריטריון, ולכן כל מנוע הכללים חל עליהן. לכל תוצאה **מצב קטלוג**: `all` / `only` (רשימת שירותים) / `none`. `fitServiceState(r,svc)` מסנן בפועל: `svcBlockReason` בהצעת הזמנים ו-`renderServiceSelect` בבחירת השירות (כרטיס מסומן, לחיצה מסורבת). שמירת השאלון מסירה מהליד שירות שכבר אינו מוצע. `fitBarHTML` מציג מה מוצע; `openCritFit` מריץ מול הלקוח עם תצוגת תוצאה חיה.
- **מנוע אוטומציות (`js/automations.js` + `automations-ui.js`, v235-236)** — כל 15 החוקים **כבויים כברירת מחדל** (`AUTO_STATE` ב-`cb_autoOn`). `autoFire(key,ctx)` נקרא מנקודות אמיתיות בקוד (h1 בנעילה, h3/h4 בשחרור, h5/h6 בסליקה, a2 בקביעת פגישה, h8 בחתימה, a1 ביצירת ליד). `AUTO_IMPL[key].run` מבצע שינוי אמיתי; `AUTO_LOG` יומן הרצות; `AUTO_BUSY` מונע לולאות. `autoTick()` לחוקים תלויי-זמן (a3/a7/h7). בונה חוקים מותאמים (`AUTO_TRIGGERS`/`AUTO_ACTIONS`).
- **חלוקת לידים (`js/lead-routing.js`, v236)** — `ROUTE_METHODS` (עומס/סבב/מיומנות/סניף/אזור), מסננים (זמינות, שעות, תקרת לידים), נפילה-לאחור (כל אחד/מנהל/ללא שיוך+התראה). `routePick(lead)` מחזיר גם הסבר ורשימת נפסלים. ההגדרות מוטמעות בפאנל של אוטומציה a1 (`cfg`/`cfgBind` hooks).
- **שכבת חוקים מתקדמת (`js/automations-rules.js`, v237-239)** — הכול כבוי כברירת מחדל:
  `AUTO_COND` תנאים (7 שדות × 4 אופרטורים) נבדקים ב-`autoFire` לפני ההרצה · `AUTO_CHAIN` שרשור עם `autoChainDepth` מוגבל ל-2 ·
  `AUTO_TIMER` סבב אוטומטי (`setInterval` → `autoTick`) · `AUTO_QUIET` חלון שקט עם `AUTO_QUEUE` ו-`autoSendToCustomer` ·
  חוקים חדשים `a8` אסקלציה (`autoOverdueMins` עם נפילה ל-SLA שלילי/טקסט) ו-`a9` קיבולת בסניפים. סה"כ 17 חוקים.
